namespace Stealth.Interact
{
    public static class NoisePresets
    {
        // Tunables you can reuse in interactables
        public const float SmallMag = 0.25f;   // e.g., open door, ladder attach
        public const float MediumMag = 0.5f;   // e.g., lever, console
        public const float LargeMag = 0.8f;    // e.g., break object

        public const float SmallRadius = 4f;
        public const float MediumRadius = 7f;
        public const float LargeRadius = 10f;
    }
}
